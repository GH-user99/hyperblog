cd proyecto1

git init (para decir cuál es la carpeta ppal. donde está el archivo)

ls -al (para ver archivos ocultos)

git status

git add (historia.txt) 

git commit -m - "Comenta los cambios hechos"

{
git config --global user.name "Andrés González"

git config --global user.email "andd011@yahoo.com"
}

git config -- list

cat historia.txt (para leer el archivo)

git status

git diff 

git log nombrearchivo.txt

git add .  - Para añadir los últimos cambios hechos

git show (para ver los cambios)